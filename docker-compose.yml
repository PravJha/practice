version: "3.8"

services:
  app:
    build: .
    ports:
      - "8080:8080"
    environment:
      - PYTHONUNBUFFERED=1
    volumes:
      - .:/app:ro
    command: ["uvicorn", "app.main:app", "--host", "0.0.0.0", "--port", "8080"]
